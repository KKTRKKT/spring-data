package me.kktrkkt.springdata.jpa_repository.enable_jpa_repsitories;

/*
@EnableJpaRepositories는 JPARepository 타입을 찾아서 빈으로 등록해주는 설정 애노테이션이다.

스프링 부트는 기본 설정파일에 선언되어 있어 따로 선언하지 않아도 된다.
basePackage 경로는 기본적으로 Application의 basePackage 경로를 따라가고, 설정이 가능하다.
 */